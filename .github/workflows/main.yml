name: Latigo CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: List environment info
      run: |
        make env
        make info
    - name: Build and push images to docker repo
      run: |
        make push-all

