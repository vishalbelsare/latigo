import latigo.clock
import datetime
import typing


def test_clock():
    # clock = latigo.clock.OnTheClockScheduler(start_time=datetime.time(hours=8, minutes=0, seconds=0), interval=datetime.timedelta(minutes=15))
    pass
