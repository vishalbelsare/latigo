import datetime
import typing

'''
class OnTheClockScheduler:
    """
    Class to provide a schedule from time of day + recurring interval as starting point
    Also provides convenience methods to make it easy to keep track of this schedule
    """

    def __init__(self, start_time: datetime.time, interval: datetime.timedelta):
        self.start_time = start_time
        self.interval = interval
        self.last_run = start_time

    def closest_start_time(now: datetime.datetime = datetime.datetime.now()) -> datetime.datetime:
        pass

    def time_left() -> datetime.timedelta:
        return closest_start_time(self.last_run)

    def its_time() -> bool:
        pass
'''
